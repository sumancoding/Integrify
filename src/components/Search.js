import React, {useState, useEffect} from 'react'
import {TextField, AppBar } from '@mui/material/';

const Search = () => {
  
  return (
    <div>
   <form action="">
      <TextField fullWidth label="Search by cities" id="fullWidth" sx={{mt:5}} />
   </form>
    </div>
  )
}

export default Search
